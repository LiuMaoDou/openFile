# FileM 0.1.9 Windows 更新说明

- 文件扫描显示全局目录总量与已处理目录数，减少“扫描一直在跑”但看不清进度的情况。
- 复用监听事件来源说明，完整核对时保留触发原因（例如监听丢失、变更量过大）。
- 扫描停滞超过 60 秒会给出提示，便于判断是否为权限/网络/云盘路径阻塞。
- 左下角显示版本号，便于核对当前运行构建。

与 0.1.8 相比，核心扫描并发策略与内容索引能力未改版。
